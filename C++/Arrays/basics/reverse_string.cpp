#include<iostream>
#include<bits/stdc++.h>
using namespace std;

string reverseWord(string str){
  
  reverse(str.begin(), str.end());  // in-built function for reversing string in c++
  
  return str;
}
